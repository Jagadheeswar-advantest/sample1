pipeline {
  agent any
  stages {
    stage ("compile"){
      steps{ 
       echo "this is a compile stage"
          }
    }
      stage ("build"){
        steps {
          echo "this is a build stage" 
        }
      }
    stage ("test"){
      steps {
    echo "this is a testing stage" 
      }
    }
}
}
